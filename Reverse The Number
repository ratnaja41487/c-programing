#include<stdio.h>
int main()
{
    int n,t;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
            int reverse=0;
            while(n>0){
            reverse=reverse*10+(n%10);
            n/=10;
        }
        printf("%d\n",reverse);
    }
    return 0;
}
