#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int x, y;
        scanf("%d %d", &x, &y);
    int z=x+y;
    if(z>6){
        printf("YES\n");
    }
    else{
        printf("NO\n");
    }
    }
}
